[project]
name = "fastapi-demo"
version = "0.1.0"

dependencies = [
  "fastapi",
  "uvicorn",
]

[tool.ruff]
line-length = 88
select = ["E", "F", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
